const styles = theme => ({
    PostContainer: {
        marginTop: '10%'
    },
    formControl: {
        margin: theme.spacing.unit,
        minWidth: 120,
    },
    FilterContainer: {
        marginBottom: 20
    }

});
export default styles;