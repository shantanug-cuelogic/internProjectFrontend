import React from 'react';
import Header from './Header/Header';
import Aux from '../../Aux'
const layout = (props) => {
    return(
        <Aux>
             <Header/>
        </Aux>
        
    );
}
export default layout;